import { FilterTemplatePipe } from './filter-template.pipe';

describe('FilterTemplatePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTemplatePipe();
    expect(pipe).toBeTruthy();
  });
});
